<?php

/**
 * Define custom twig Filters
 */

$app = get_app();

/**
 * Sample
 *
 * $rot13 = new Twig_SimpleFilter('rot13', function ($string) {
 *     return str_rot13($string);
 * });
 *
 * $app->twig()->addFilter($rot13);
 */